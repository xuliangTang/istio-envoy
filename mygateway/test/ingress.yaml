apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: envoytest
spec:
  rules:
    - host: envoy.virtuallain.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: myservicea
                port:
                  number: 80